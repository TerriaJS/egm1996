{
  "name": "earthgravitymodel1996",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "main": "./dist/EarthGravityModel1996.js",
  "types": "./dist/EarthGravityModel1996.d.ts",
  "devDependencies": {
    "typescript": "^3.6.3",
    "@types/node": "^12.0.2"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "postpublish": "bash -c \"git tag -a ${npm_package_version} -m \"${npm_package_version}\" && git push origin ${npm_package_version}\""
  }
}
